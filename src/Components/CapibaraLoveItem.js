import React, {useContext} from 'react';
import {Context} from "../index";
import {Container} from "react-bootstrap";

const CapibaraLoveItem = () => {

    const {currentUser} = useContext(Context)

    return (
        <Container>

        </Container>
    );
};

export default CapibaraLoveItem;